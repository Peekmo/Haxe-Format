package data;

/**
 * Contains all possible status
 *
 * @author Axel Anceau (Peekmo)
 **/
enum StatusEnum
{
    NONE;
    COMMENT;
    ANNOTATION;       
    DECLARATION;
    NAME;
    STRING;
    FUNCTION;
    METADATA;
    DESCRIPTION;
}